# Frontend API URL configuration.
# For local development with a local backend, set this to http://localhost:8001
# If left empty, it defaults to '/api' which uses the proxy configured in vite.config.ts.
VITE_API_URL=http://localhost:8001
